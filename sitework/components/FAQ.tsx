const categories=[
{id:"basics",label:"Basics",faqs:[["What is Parmana?","Parmana helps businesses control actions taken by software and AI. It checks an action before it happens and keeps the full story of how that action was allowed."],["What problem does it solve?","As AI starts doing more work, businesses need to know that important actions still follow business decisions. Parmana provides that check."],["Does Parmana make business decisions?","No. Your business decides what is allowed. Parmana checks that the software follows that decision."]]},
{id:"how",label:"How it works",faqs:[["What is the five-part chain?","Declaration → Policy → Decision → Authorization → Execution. Each step is connected so you can check where an action came from and what happened."],["What happens when an action is allowed?","The action can continue to the business system. Parmana keeps a record showing what was allowed and what happened."],["What happens when an action is not allowed?","The action is stopped."],["What if the check cannot be completed?","The action is not treated as allowed. It is held until it can be checked."]]},
{id:"business",label:"For businesses",faqs:[["What actions can Parmana protect?","Examples include payments, refunds, access changes, production changes, data deletion, software releases, contracts and vendor actions."],["Do we need to replace our existing systems?","No. Parmana is designed to work with the systems you already use."],["Who decides what is allowed?","Your business does. Parmana does not decide what your business should allow."]]},
{id:"pilot",label:"Pilot",faqs:[["How do we start?","Start with one important action and the business rule that should control it. Test it first, then expand."],["Do we need production data?","No. A pilot can start in a test environment."],["What does a pilot show?","It shows whether a real business rule can control a real workflow and whether the resulting record is useful to your team."]]}];
export default function FAQ(){return <section id="faq" className="bg-paper border-b border-border"><div className="max-w-container mx-auto px-6 py-16 md:py-20 lg:py-24"><h2 className="text-[26px] md:text-[32px] font-bold leading-[1.3] tracking-tight text-ink text-center max-w-[760px] mx-auto">Questions teams ask</h2><div className="mt-8 flex flex-wrap justify-center gap-2 max-w-[800px] mx-auto">{categories.map(c=><a key={c.id} href={`#faq-${c.id}`} className="rounded-md border border-border bg-white px-4 py-2 text-xs md:text-sm font-medium text-ink/70">{c.label}</a>)}</div><div className="mt-12 max-w-[800px] mx-auto space-y-12">{categories.map(c=><div key={c.id} id={`faq-${c.id}`} className="scroll-mt-24"><p className="text-xs font-semibold uppercase tracking-[0.15em] text-purple-deep mb-2">{c.label}</p><div className="divide-y divide-border border-y border-border">{c.faqs.map(([q,a])=><details key={q} className="group py-5"><summary className="flex cursor-pointer list-none items-center justify-between gap-6 text-base md:text-lg font-semibold text-ink"><span>{q}</span><span className="shrink-0 text-2xl font-normal text-purple-deep group-open:rotate-45" aria-hidden="true">+</span></summary><p className="mt-4 max-w-[720px] text-sm md:text-base leading-[1.6] text-ink/70">{a}</p></details>)}</div></div>)}</div></div></section>}
