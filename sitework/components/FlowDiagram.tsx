export default function FlowDiagram(){const steps=["Declaration","Policy","Decision","Authorization","Execution"];return <div className="mx-auto max-w-[760px] py-4"><div className="grid grid-cols-1 md:grid-cols-5 gap-3">{steps.map((s,i)=><div key={s} className="rounded-md border border-border bg-lavender p-5 text-center"><p className="text-xs font-mono uppercase tracking-wide text-purple-deep">Step {i+1}</p><p className="mt-2 text-sm font-bold text-ink">{s}</p></div>)}</div><p className="mt-8 text-center text-sm md:text-base leading-[1.7] text-ink/70">The business declares its authority, sets its rules, makes a decision, allows the specific action, and then the system carries it out.</p></div>}
