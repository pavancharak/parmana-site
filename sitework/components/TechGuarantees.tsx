const points=[["Only allowed actions run","An action that does not meet the business's conditions is stopped."],["The full chain stays connected","You can trace an action back through its permission, decision, rules and declared authority."],["The result can be checked","The record is designed so another system or person can check what was allowed and what happened."],["The business stays in charge","Parmana does not make business decisions. It helps enforce the decisions the business makes."]];
export default function TechGuarantees(){return <section id="guarantees" className="bg-lavender border-b border-border"><div className="max-w-container mx-auto px-6 py-16 md:py-20 lg:py-24"><h2 className="text-[26px] md:text-[36px] font-bold leading-[1.2] tracking-tight text-ink text-center max-w-[820px] mx-auto">What you can rely on</h2><div className="mt-12 grid grid-cols-1 md:grid-cols-2 gap-6">{points.map(([title,body])=><div key={title} className="rounded-md border border-border bg-white p-8"><h3 className="text-base font-bold text-ink">{title}</h3><p className="mt-3 text-sm leading-[1.7] text-ink/70">{body}</p></div>)}</div></div></section>}
