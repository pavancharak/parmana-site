const signals=[["AI is doing more","AI is moving from answers to actions inside business software."],["Business decisions still matter","The business decides what can be approved, changed, paid, shared or released."],["Control must come first","A record after the fact cannot stop an action that should never have happened."]];
export default function Evidence(){return <section id="evidence" className="bg-paper border-b border-border"><div className="max-w-container mx-auto px-6 py-16 md:py-20 lg:py-24"><h2 className="text-[28px] md:text-[40px] font-bold leading-[1.15] tracking-tight text-ink text-center max-w-[820px] mx-auto">Why this matters now</h2><p className="mt-4 text-base md:text-lg text-ink/70 text-center max-w-[720px] mx-auto">As software takes on more work, the business needs to stay in charge of what that software can do.</p><div className="mt-12 grid grid-cols-1 md:grid-cols-3 gap-6">{signals.map(([title,body])=><div key={title} className="rounded-md border border-border bg-lavender p-7"><h3 className="text-base font-bold text-ink">{title}</h3><p className="mt-3 text-sm leading-[1.7] text-ink/70">{body}</p></div>)}</div></div></section>}
